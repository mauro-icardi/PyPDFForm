Commit 3:
"HVLSN-48 Fixed minor performance issues."

def any_image_to_jpg(image_stream: bytes) -> bytes:
    """Converts an image of any type to jpg."""
    
    
    buff = BytesIO()
    buff.write(image_seam)
 
        _file.seek(0)
        result = _file.read()

    buff.close()
    return result
